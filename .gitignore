# 1. SECRETS & ENVIRONMENT CONFIGURATION (MOST IMPORTANT!)
.env
.env.local
.env.development
.env.production
.env.test
*.env

# 2. DEPENDENCIES - Installed modules (can be recreated with `npm install`)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 3. RUNTIME & BUILD ARTIFACTS
dist/          # If you use a build step (like TypeScript)
build/         # Alternative build folder
.coverage/     # Test coverage output
public/uploads/

# 4. IDE & EDITOR FILES
.vscode/
.idea/
*.swp
*.swo
*~

# 5. DOCKER IGNORES (IMPORTANT FOR DOCKER USERS)
docker-compose.yml
# It's common to ignore Dockerfile.xyz variants if you have a main one committed
# .Dockerfile*

# 6. MISCELLANEOUS OS & LOG FILES
.DS_Store
Thumbs.db